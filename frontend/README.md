# Ain-Bondhu Frontend

## Run

```powershell
npm install
npm run dev
```

Open: http://localhost:3000

## Notes

- Offline demo scenarios in `src/data/scenarios.json`.
- Health check uses `/api/health` and proxies to `BACKEND_BASE_URL`.
